# Wealth-OS
The Unified Investment System.

## Structure
- **backend/**: The Python Brain (FastAPI + Pandas).
  - **core/**: Universal Math Engine (applies same logic to Crypto & Stocks).
- **frontend/**: The Glass Interface (React + Vite).

## Quick Start
Run `WakeUp.bat` to launch the system.
